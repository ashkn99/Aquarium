services:
  - type: web
    name: aquarium-assistant
    runtime: python
    plan: free
    buildCommand: pip install -e ".[postgres]"
    startCommand: uvicorn aqua_assistant.api.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        sync: false
